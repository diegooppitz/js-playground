export type ProductData = {
    year: string;
    fiscalState: string;
    baseValue: number;
    totalValue: number;
};

export type NewProductData = {
    year: string;
    fiscalState: string;
    baseValue: number;
};
